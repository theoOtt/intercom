{
  "name": "session-chat-bridge",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Claude Code and Codex multi-session chat over a shared SQLite file.",
  "scripts": {
    "test": "node --no-warnings test-db.mjs && node --no-warnings test-mcp.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
