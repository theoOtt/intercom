---
description: Install or refresh Intercom's shared database and shell launch integration
allowed-tools: Bash(node:*)
---

Run the plugin's recoverable local setup command:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/setup.mjs"
```

Report the backup directory and restart instructions printed by the command. Do not modify any
other configuration manually.
